import { Card } from '@brushbox/ui/Card';
import { Button } from '@brushbox/brushbox';

export default function Page() {
  return (
    <main>
      <Card />
      <Button>Click</Button>
    </main>
  );
}
